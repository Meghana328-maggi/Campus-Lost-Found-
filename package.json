{
  "name": "campus-lost-and-found",
  "version": "1.0.0",
  "description": "Comprehensive MERN Stack Campus Lost & Found Platform",
  "main": "server/src/server.js",
  "scripts": {
    "start": "node server/src/server.js",
    "build": "npm install --prefix server",
    "build:server": "npm install --prefix server",
    "build:client": "npm install --prefix client && npm run build --prefix client",
    "build:all": "npm install --prefix server && npm install --prefix client && npm run build --prefix client",
    "postinstall": "npm install --prefix server",
    "install:all": "npm install && npm install --prefix server && npm install --prefix client",
    "server": "npm run dev --prefix server",
    "client": "npm run dev --prefix client",
    "dev": "node ./node_modules/concurrently/dist/bin/cli.js --kill-others-on-fail \"npm run server\" \"npm run client\"",
    "seed": "npm run seed --prefix server",
    "test:server": "npm test --prefix server",
    "test:client": "npm test --prefix client",
    "test": "npm run test:server"
  },
  "keywords": [
    "mern",
    "lost-and-found",
    "campus",
    "react",
    "nodejs",
    "mongodb",
    "express",
    "socketio"
  ],
  "author": "Campus Lost & Found Team",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.4.7"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
